init("0", 1); --以当前应用 Home 键在右边初始化

require "util"
require "init"

function setCount()
	if count == 1 then
		count = 0;
	elseif count == 2 then
		count = 0;
	elseif count == 3 then
		count = 0;
	end
end